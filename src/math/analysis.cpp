

float getEMA(float open[], float close[], int periods) {
    float EMA[periods];

    return EMA[];
}

float getSMA(float open[], float close[], int periods) {
    float SMA[periods];

    return SMA[];
}

int getTrend(float avg1, float avg2) {

    // -1 down | 0 | 1 up
    return 0;
}

float averagePeriods(float minInPeriod = 30.0) {
    float averagesForPeriod[];
    // Ensure periods are sorted by date, time
    // While history remains, Loop through each period
    // Average period
    // "2018-01-30 01:11:50"

    return averagesForPeriod[];
}
